import { type Ref } from 'vue-demi';
import type { ViewportOptions, ViewportQuery } from '../types.js';
export declare const STATE_KEY = "viewportState";
export declare function createViewportManager(options: ViewportOptions, state: Ref<string>): {
    breakpoint: import("vue").WritableComputedRef<string>;
    breakpointValue: (searchBreakpoint: string) => number;
    isGreaterThan: (searchBreakpoint: string) => boolean;
    isGreaterOrEquals: (searchBreakpoint: string) => boolean;
    isLessThan: (searchBreakpoint: string) => boolean;
    match: (breakpointToMatch: string) => boolean;
    matches: (...breakpointsToMatch: string[]) => boolean;
    queries: import("vue").ComputedRef<Record<string, ViewportQuery>>;
};
